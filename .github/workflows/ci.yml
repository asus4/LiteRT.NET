name: ci

on:
  # Disable auto triggers until stable version
  # push:
  #   branches: [main]
  # pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
        with:
          path: litert-net

      # Needed by fetch-natives.sh for the prebuilt core / GPU / Gemma plugin.
      - name: Checkout LiteRT-LM
        uses: actions/checkout@v6
        with:
          repository: google-ai-edge/LiteRT-LM
          ref: main
          path: LiteRT-LM

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: |
            8.0.x
            10.0.x

      - name: Fetch natives
        working-directory: litert-net
        run: |
          chmod +x scripts/fetch-natives.sh
          LITERT_LM_DIR="$GITHUB_WORKSPACE/LiteRT-LM" scripts/fetch-natives.sh

      - name: Restore
        working-directory: litert-net
        run: dotnet restore

      - name: Build (managed)
        working-directory: litert-net
        run: |
          dotnet build --no-restore -c Release "src/LiteRT/LiteRT.csproj"
          dotnet build --no-restore -c Release "src/LiteRT.LM/LiteRT.LM.csproj"

      - name: Pack
        working-directory: litert-net
        run: |
          dotnet pack -c Release -o artifacts "src/LiteRT.Native/LiteRT.Native.csproj"
          dotnet pack -c Release -o artifacts "src/LiteRT.Gpu.Metal.Native/LiteRT.Gpu.Metal.Native.csproj"
          dotnet pack -c Release -o artifacts "src/LiteRT.Gpu.WebGpu.Native/LiteRT.Gpu.WebGpu.Native.csproj"
          dotnet pack -c Release -o artifacts "src/LiteRT.Gpu.OpenCl.Native/LiteRT.Gpu.OpenCl.Native.csproj"
          dotnet pack -c Release -o artifacts "src/LiteRT.LM.Native/LiteRT.LM.Native.csproj"
          dotnet pack -c Release -o artifacts "src/LiteRT/LiteRT.csproj"
          dotnet pack -c Release -o artifacts "src/LiteRT.LM/LiteRT.LM.csproj"

      - name: Upload packages
        uses: actions/upload-artifact@v7
        with:
          name: nupkg
          path: litert-net/artifacts/*.nupkg
